<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.aurifolia.cloud.id.infrastructure.snowflake.mapper.SnowflakeNodeMapper">
    
    <resultMap id="SnowflakeNodeResultMap" type="org.aurifolia.cloud.id.infrastructure.snowflake.po.SnowflakeNodePO">
        <id property="id" column="id"/>
        <result property="bizTag" column="biz_tag"/>
        <result property="machineId" column="machine_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    
    <select id="selectByBizTagForUpdate" resultMap="SnowflakeNodeResultMap">
        SELECT id, biz_tag, machine_id, create_time, update_time
        FROM snowflake_node
        WHERE biz_tag = #{bizTag}
        FOR UPDATE
    </select>
    
    <select id="selectByBizTag" resultMap="SnowflakeNodeResultMap">
        SELECT id, biz_tag, machine_id, create_time, update_time
        FROM snowflake_node
        WHERE biz_tag = #{bizTag}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO snowflake_node (biz_tag, machine_id, create_time, update_time)
        VALUES (#{bizTag}, #{machineId}, #{createTime}, #{updateTime})
    </insert>
    
    <update id="update">
        UPDATE snowflake_node
        SET machine_id = #{machineId}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    
</mapper>
